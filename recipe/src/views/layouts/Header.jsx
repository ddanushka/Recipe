import SearchBox from "../components/SearchBox";

function Header() {
  return (
    <section className="header">
        <h1>sample header</h1>
        <SearchBox/>
    </section>
  );
}

export default Header;
